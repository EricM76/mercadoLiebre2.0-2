<!DOCTYPE html>
<html lang="en">
<%- include('partials/head') %>

    <body>


        <%- include('partials/header') %>
            <div class="container">
                <section class="detalle">
                    <div class="row productos">
                        <div class="titulo col-12">
                            <h2>
                                <%= title %>: 
                                <%= producto.name%>
                            </h2>
                        </div>
                        <div class="col-12">
                            <div class="contenido text-left">
                                <div class="row">
                                    <div class="col-12 col-lg-8">
                                        <figure class="text-center">
                                            <img src="/images/products/<%= producto.imagenes %> " alt="" class="img-fluid w-50" >
                                        </figure>
                                        <p>
                                            <%= producto.descripcion %>
                                        </p>
                                    </div>
                                    <div class="col-12 col-lg-4 datos">
                                        <h3 class="my-4">
                                            <%= producto.nombre %>
                                        </h3>
                                        <% if (producto.descuento != 0) { %>
                                            <span><strike>$<%= producto.precio.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".") %></strike> / <strong><%- producto.descuento %>% OFF</strong></span>
                                            <% } %>
                                                <h2>$
                                                    <%- (producto.precio * producto.descuento / 100).toFixed(0).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".") %>
                                                </h2>
                                                <p><i class="fas fa-credit-card mr-3 my-4"></i>Pagá en 12 cuotas sin interés</p>
                                                <p><i class="fas fa-store mr-3 my-4"></i>Retiro gratis en locales del vendedor</p>
                                                <button>Comprar ahora</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </section>
            </div>
            <%- include('partials/footer') %>
    </body>

</html>